Front-end project for NORUTIN application
(server: https://github.com/Lackier/norutin-server)

The project uses:
- React
- bootstrap library

To develop run:
-npm install

To build and deploy the application run:
-npm start
