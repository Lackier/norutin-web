import React from "react"
import { useLocation } from "react-router-dom";

export default function DeskList() {
    const location = useLocation();
    const desks = location.state

    debugger;

    return (
        <>

        </>
    )
}